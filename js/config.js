export const CONFIG = {
    // Google Apps Script Web App URL (Injected during build)
    SHEET_API_URL: '__SHEET_API_URL_PLACEHOLDER__',
    // Set to false to use Google Sheets, true to use localStorage for testing
    USE_LOCAL_STORAGE: false
};
